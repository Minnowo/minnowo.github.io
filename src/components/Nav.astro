---
import { SITE, NAV } from '../site.config';
---

<nav class="navbar">
    <div class="navbar-collapse nav_pad">
        <a class="navbar-brand container-transition" href="/">
            <img src="/favicon.png" alt="">
            {SITE.name}
        </a>

        <div class="navbar-nav container-transition">
            {NAV.map((item) => (
                <a class="nav-item nav-link link-fade" href={item.href}>{item.label}</a>
            ))}
        </div>
    </div>
</nav>
