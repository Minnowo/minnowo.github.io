---
import Shell from './Shell.astro';

export interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Shell title={title}>
    <div class="card">
        <slot />
    </div>
</Shell>
