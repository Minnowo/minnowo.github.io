---
import Shell from './Shell.astro';

export interface Props {
  title?: string;
}

const { title } = Astro.props;
---

<Shell title={title}>
    <div class="rounded-[10px] border border-c-surface1 bg-c-surface0 p-5">
        <slot />
    </div>
</Shell>
